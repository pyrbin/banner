[todo]

- fix validation errors
- memory cleanup of vulkan objects
- window class that wraps glfw window
- initial engine class (@see liblava app.hpp for inspiration)
- cleaner shader loading

- lookup deps for model rendering & input?
- render a model

- add imgui
- fix glw3.dll dependency, currently i just copied the dll to the build folder